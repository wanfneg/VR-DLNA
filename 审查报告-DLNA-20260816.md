# VR-DLNA 服务器深度审查报告（2026-08-16）

审查对象：`vr_dlna.py`（Python 标准库 DLNA MediaServer，服务 cd2/115 云盘挂载给 Quest DeoVR）
审查方式：6 路并行独立审查代理（并发/协议/安全/云盘容错/HTTP 语义/GUI 配置）+ 复现验证 + 回归测试
结果：共发现 **65 项**（去重后约 50 项真实问题），本次修复 **高/中危 24 项**，其余为已知取舍或低危留档。

---

## 一、已修复（高危 9 项）

| # | 问题 | 修复 |
|---|------|------|
| 1 | `_dir_items` 排序 key 里未保护的 `is_dir()`：单个坏条目抛错会把**整个目录**误拉黑 + 返回空 | 排序只按文件名；is_dir 移到受保护循环（`_dir_item_list`） |
| 2 | 每个视频条目独立 `stat()` + 每视频一次 `iterdir()` 找字幕：324 视频 = 326 次目录扫描 | 复用本次枚举的 `sibling_names`；`_find_subtitles` 内存匹配 |
| 3 | `_stream_file` 先发响应头后 open：打开失败=声明 Content-Length 却 0 字节 | 先 open+seek 成功再发头；失败回 404 |
| 4 | 流中途 EOF 早于声明长度：keep-alive 下客户端永久挂起 | 提前 EOF 时 `Connection: close` + 告警日志 |
| 5 | `.strm` 代理丢 `Location`（3xx 无法跟随）| Location/Content-Encoding 透传 |
| 6 | `.strm` 代理上游 chunked（无 Content-Length）：客户端读不到结束信号 | close-delimited 转发（`Connection: close`）|
| 7 | `.strm` 代理头已发后失败仍写 502 污染媒体流 | `headers_sent` 标志：头后失败只关连接 |
| 8 | GUI 多实例并存：配置互相覆盖 + 同端口/UUID 双服务（本次会话实测发生） | `CreateMutexW` 单实例互斥 + label 去重 |
| 9 | GUI 端口 0/负数/超范围：OverflowError 静默崩溃 / SSDP 广告无效端口 | 1~65535 显式校验 + 启动异常可见化 |

## 二、已修复（中危 15 项）

| # | 问题 | 修复 |
|---|------|------|
| 10 | 黑名单 set 无锁并发读写：`Set changed size during iteration` → 500；写盘竞争 | `threading.Lock` + 仅新增时写 + 临时文件原子替换 |
| 11 | 黑名单 TTL 只在启动时过滤：运行期永不过期（恢复的目录一直隐藏） | 内存存时间戳，`_is_broken` 惰性 TTL 剔除 |
| 12 | `_os_retry` 对永久错误（FileNotFound 等）也重试 4 次共 4.8s；退避无抖动 | 只重试瞬时错误码（WinError 1005 等）；抖动退避；3 次×0.5s |
| 13 | 字幕匹配 `split('.')[0]` 过度截断：跨视频误挂字幕、lang 错乱 | 完整 stem 匹配（`Movie.2024.2160p.zh` → lang=zh）|
| 14 | 多根 label 重名（盘符根 D:\ E:\ 都是 "Videos"）：第二个根完全不可达 | `DlnaApp.__init__` label 强制唯一（后缀 -2/-3）+ casefold 匹配 |
| 15 | 单根模式下收到陈旧多根 key（label/rel）：路径翻倍静默错文件 | 单根剥离 label 前缀 |
| 16 | Windows ADS 冒号（`file.mp4:Zone.Identifier`）可读备用数据流 | key 含 `:` 直接拒绝 |
| 17 | 词法 containment 防不了根内 junction/symlink 逃逸 | 最终组件 reparse point 检测拒绝 |
| 18 | `.strm` 代理 SSRF：可访问内网/云元数据（169.254.169.254）| `_is_public_http_target`：拒绝 loopback/私网/link-local/组播 |
| 19 | 不可满足 Range（越界 seek）返回 200 全量（10GB 重下）| 416 + `Content-Range: bytes */total`；多段 Range 拒绝；`bytes=-0` 拒绝 |
| 20 | Browse 分页未实现：大目录总返全量；BrowseMetadata 容器返回子条目 | `StartingIndex/RequestedCount` 分页 + `TotalMatches` 复用枚举结果 |
| 21 | HEAD /subs/ 下载整个字幕文件 | HEAD 只 stat |
| 22 | 未知 SOAP action 回纯文本 501；CM SCPD 缺 action；eventSubURL 声明了未实现 | SOAP Fault 401；`_soap_fault`；移除 eventSubURL；CM 补齐 GetCurrentConnectionIDs/Info |
| 23 | GetProtocolInfo Source 与实际 MIME 不一致（缺 mp2t/wmv/flv；广告了不可达的 mpeg）| 对齐 mime_for 表；VIDEO_EXTS 补 .mpg/.mpeg/.3gp/.ogv/.vob |
| 24 | tkinter 跨线程写日志（GUI 偶发崩溃）；访问日志无锁、无轮转、SOAP body 全量落盘 | GuiHandler 队列+主线程轮询；`_write_access_log` 加锁 + 5MB 轮转 + body 截断 400 字符 |

## 三、其他加固

- `DlnaHandler.timeout = 60`：空闲 keep-alive 连接回收（防线程泄漏）
- HTTP/1.0 keep-alive 显式 `Connection: keep-alive`；关闭时回 `Connection: close`
- `do_OPTIONS` 200 + Allow（部分控制点先探测）
- SSDP：recv 0.5s 超时（stop 确定性退出）；关闭时广播 byebye；uuid 精确匹配不再回应别的设备
- 配置加载 `isdir` 加重试（云盘打盹误报 False 时保留原配置，不静默清空）；空 roots 拒绝保存
- 配置保存原子写（tmp + os.replace）；写失败 log 提示
- `contentFeatures.dlna.org` 无 profile 时省略 PN 字段
- 字幕响应 Content-Type 对齐 DIDL 声明（application/x-subrip 等）

## 四、已知取舍（未修，留档）

| 项 | 说明 |
|----|------|
| res 缺 duration/resolution/bitrate | 零探测设计使然（云盘上 ffprobe 会超时），DeoVR 靠流嗅探 |
| 未知 ObjectID 返回空 DIDL 而非 701 错误 | 刻意兼容 DeoVR contentProc（空 Result 会 LoadXml(null) 崩溃）|
| container 缺 childCount | 补全需逐个枚举子目录（云盘昂贵）；DeoVR 不依赖 |
| 流式 socket 超时 | handler 60s 超时对长流也生效：客户端停滞 >60s 会被断开（回收线程，利大于弊）|
| `.strm` 代理禁内网目标 | 如需代理内网源可后续加白名单配置 |
| `_send_error_text` 对 HEAD 仍带 body 的边角 | 已给 _route 错误路径传 want_body；个别深层路径未全覆盖 |
| stop 后已建立的流连接继续服务至断开 | ThreadingHTTPServer daemon 线程语义；文档化即可 |

## 五、测试覆盖

- `_test_audit_fixes.py`：26 项回归（分页/黑名单/TTL/字幕/key 安全/label/Range/重试/SSRF）
- `_test_http_smoke.py`：HTTP 端到端（SOAP 分页/未知 action Fault/HEAD/Range 206/416/OPTIONS/404）
- `_test_real_volume.py`：真实 O: 云盘卷浏览验证
- 全部通过；服务器已用新代码重启（单实例，端口 1919）

## 六、遗留观察

- 云盘侧坏条目（如 `DSVR\dsvr`）已从 115 消失（cd2 重新同步后目录从 324 变为 295 项）——黑名单自动失效，无需干预
- 长时间运行建议关注 `vr_dlna_access.log` 轮转（5MB）与 `vr_dlna_broken_dirs.json`（24h TTL）
